postfix notation calculators

the `:0` command takes two arguments: the first value to try, and the function to find a root of, in terms of `.`

the `.` expression normally is ans, the previous computed value
